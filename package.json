{
  "name": "proyecto-ganadero",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "corepack pnpm --filter web dev",
    "build": "corepack pnpm --filter web build",
    "preview": "corepack pnpm --filter web preview",
    "dev:api": "corepack pnpm --filter api start:dev",
    "build:api": "corepack pnpm --filter api build",
    "test:api": "corepack pnpm --filter api test"
  },
  "engines": {
    "node": ">=20"
  }
}
